# Pinned env per EXECUTION_GUIDELINE.md Section 3.2 (Python 3.10/3.11, CPU).
# NOTE: install full stack on Kaggle notebooks (compatible Python + GPU).
# Local Python here is 3.12 — TF 2.15 wheels do not exist for 3.12, so a
# full local install is expected to fail; local machine is for Node/Ganache
# + lightweight work only.
tensorflow-cpu==2.15.0
tensorflow-privacy==0.9.0
keras==2.15.0
fastapi==0.110.0
uvicorn==0.27.1
pydantic==1.10.14
scikit-learn==1.4.0
pandas==2.1.4
numpy==1.26.4
matplotlib==3.8.2
seaborn==0.13.2
requests==2.31.0
web3==6.15.1
python-multipart==0.0.9
